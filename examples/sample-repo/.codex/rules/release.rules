# Production releases need a human decision.
prefix_rule(
  pattern = ["git", "push"],
  decision = "prompt",
  justification = "Use the reviewed release workflow instead",
)
prefix_rule(pattern = ["rm", "-rf"], decision = "forbidden")
