[
  {
    "id": "demo-resolves",
    "claim": "The bundled sample resolves four policy files into nine effective rules and one shadowed rule.",
    "where": "landing preview and demo summary",
    "test": "npm test -- --grep @claim:demo-resolves",
    "sandbox": "fresh browser context at /demo using bundled sample data"
  },
  {
    "id": "report-formats",
    "claim": "Permit Map prints terminal, JSON, and Markdown reports.",
    "where": "landing how-it-works section and README",
    "test": "npm test -- --grep @claim:report-formats",
    "sandbox": "bundled examples/sample-repo in a clean local process"
  },
  {
    "id": "policy-files",
    "claim": "Permit Map reads known policy paths instead of scanning repository and credential files.",
    "where": "landing facts, privacy page, and README",
    "test": "npm test -- --grep @claim:policy-files",
    "sandbox": "temporary repository containing a policy file, a decoy .env, and an unrelated JSON file"
  },
  {
    "id": "no-account",
    "claim": "The demo and CLI run without an account.",
    "where": "landing facts and privacy page",
    "test": "npm test -- --grep @claim:no-account",
    "sandbox": "fresh browser context with no cookies or saved state"
  },
  {
    "id": "mit-license",
    "claim": "Permit Map is free under the MIT license.",
    "where": "landing facts, terms page, and README",
    "test": "npm test -- --grep @claim:mit-license",
    "sandbox": "clean repository checkout; inspect LICENSE and Cargo package metadata"
  },
  {
    "id": "browser-privacy",
    "claim": "The website sets no cookies, stores no browser data, and makes no cross-origin demo requests.",
    "where": "demo banner and privacy page",
    "test": "npm test -- --grep @claim:browser-privacy",
    "sandbox": "fresh browser context; capture every request and inspect browser storage after reset"
  },
  {
    "id": "cli-local",
    "claim": "The CLI does not launch agents and has no network or telemetry client.",
    "where": "privacy page and README",
    "test": "npm test -- --grep @claim:cli-local",
    "sandbox": "clean repository checkout; inspect runtime dependencies and Rust source for network clients"
  },
  {
    "id": "resolution-order",
    "claim": "Claude exact matches resolve deny, then ask, then allow across every discovered scope.",
    "where": "landing steps, demo notes, reports, and README",
    "test": "npm test -- --grep @claim:resolution-order",
    "sandbox": "temporary repository with duplicate exact matchers across and within layers"
  },
  {
    "id": "codex-context",
    "claim": "Codex project policy stays unresolved until its trust context is supplied; trusted nested project files override parent project files.",
    "where": "landing steps, demo notes, reports, and README",
    "test": "npm test -- --grep @claim:codex-context",
    "sandbox": "temporary Git-like repository with root and nested .codex/config.toml files"
  },
  {
    "id": "codex-rules",
    "claim": "Permit Map reads documented multiline Codex command rules, expands union patterns, defaults a missing decision to allow, and resolves exact matches forbidden, then prompt, then allow.",
    "where": "README resolution rules and report adapter notes",
    "test": "npm test -- --grep @claim:codex-rules",
    "sandbox": "temporary trusted repository with multiline, union, default, forbidden, prompt, and allow .rules entries"
  },
  {
    "id": "vendor-policy-safe",
    "claim": "Permit Map refuses to write a report over a discovered vendor policy file.",
    "where": "landing boundaries and README",
    "test": "npm test -- --grep @claim:vendor-policy-safe",
    "sandbox": "temporary repository; request Markdown output at .claude/settings.json and assert the original JSON remains"
  },
  {
    "id": "vendor-settings-unchanged",
    "claim": "Permit Map does not change vendor settings.",
    "where": "landing boundaries",
    "test": "npm test -- --grep @claim:vendor-settings-unchanged",
    "sandbox": "temporary repository with every supported project policy file; hash each file before and after a successful trusted inspection"
  },
  {
    "id": "cli-errors",
    "claim": "Missing paths and malformed supported policy files exit with code 2.",
    "where": "README usage",
    "test": "npm test -- --grep @claim:cli-errors",
    "sandbox": "temporary repository with a missing path and malformed Claude settings JSON"
  },
  {
    "id": "vendor-boundaries",
    "claim": "Different vendors never shadow one another, and overlapping patterns remain visible.",
    "where": "README resolution rules and landing boundaries",
    "test": "npm test -- --grep @claim:vendor-boundaries",
    "sandbox": "temporary repository with Claude patterns and a Codex command rule"
  },
  {
    "id": "touch-targets",
    "claim": "Visible home-page links meet the 44 px touch-target baseline at 390 px.",
    "where": "site accessibility contract",
    "test": "npm test -- --grep @claim:touch-targets",
    "sandbox": "390 × 844 browser viewport; measure each visible home-page link"
  },
  {
    "id": "demo-isolated",
    "claim": "The demo command changes nothing outside its temporary directory.",
    "where": "README sandbox section and CLI stderr",
    "test": "npm test -- --grep @claim:demo-isolated",
    "sandbox": "fresh caller directory with a sentinel file; run the built demo binary and verify the caller remains unchanged"
  }
]
